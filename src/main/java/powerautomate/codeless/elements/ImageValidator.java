package powerautomate.codeless.elements;

import org.openqa.selenium.WebDriver;

public class ImageValidator implements Element{
    @Override
    public boolean execute(WebDriver webDriver) {
        return true;
    }
}
